// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: vortyx/optic/v1/service.proto

package opticv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/abdul/vortyx/backend/gen/proto/go/vortyx/optic/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// OpticServiceName is the fully-qualified name of the OpticService service.
	OpticServiceName = "vortyx.optic.v1.OpticService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// OpticServiceGetStatusProcedure is the fully-qualified name of the OpticService's GetStatus RPC.
	OpticServiceGetStatusProcedure = "/vortyx.optic.v1.OpticService/GetStatus"
)

// OpticServiceClient is a client for the vortyx.optic.v1.OpticService service.
type OpticServiceClient interface {
	GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error)
}

// NewOpticServiceClient constructs a client for the vortyx.optic.v1.OpticService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewOpticServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) OpticServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	opticServiceMethods := v1.File_vortyx_optic_v1_service_proto.Services().ByName("OpticService").Methods()
	return &opticServiceClient{
		getStatus: connect.NewClient[v1.GetStatusRequest, v1.GetStatusResponse](
			httpClient,
			baseURL+OpticServiceGetStatusProcedure,
			connect.WithSchema(opticServiceMethods.ByName("GetStatus")),
			connect.WithClientOptions(opts...),
		),
	}
}

// opticServiceClient implements OpticServiceClient.
type opticServiceClient struct {
	getStatus *connect.Client[v1.GetStatusRequest, v1.GetStatusResponse]
}

// GetStatus calls vortyx.optic.v1.OpticService.GetStatus.
func (c *opticServiceClient) GetStatus(ctx context.Context, req *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error) {
	return c.getStatus.CallUnary(ctx, req)
}

// OpticServiceHandler is an implementation of the vortyx.optic.v1.OpticService service.
type OpticServiceHandler interface {
	GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error)
}

// NewOpticServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewOpticServiceHandler(svc OpticServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	opticServiceMethods := v1.File_vortyx_optic_v1_service_proto.Services().ByName("OpticService").Methods()
	opticServiceGetStatusHandler := connect.NewUnaryHandler(
		OpticServiceGetStatusProcedure,
		svc.GetStatus,
		connect.WithSchema(opticServiceMethods.ByName("GetStatus")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vortyx.optic.v1.OpticService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case OpticServiceGetStatusProcedure:
			opticServiceGetStatusHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedOpticServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedOpticServiceHandler struct{}

func (UnimplementedOpticServiceHandler) GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vortyx.optic.v1.OpticService.GetStatus is not implemented"))
}
